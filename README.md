# 线索收集官网与后台管理 MVP

这是一个可直接本地运行的最小应用，统一提供：

- 首页 `/`
- 后台 `/admin`
- 线索接口 `/api/*`
- 本地 JSON 存储 `data/leads.json`

## 启动

```bash
npm start
```

默认监听 `http://127.0.0.1:3000`。

## 验证

```bash
npm test
```

测试会验证：

1. 首页可打开
2. 表单接口校验生效
3. 提交线索后后台可查看
4. 后台状态更新后重新查询可见
